package com.example.stock;

import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.TextField;

public class gestionStock {
/*
    protected TextField txtEmplacement;
    protected TextField txtFournisseur;
    protected TextField txtCategorie;
    protected TextField txtReference;
    protected TextField txtDesignation;
    protected TextField txtQuantite;
    protected TextField txtEtat;
    protected TextField txtObservation;

    protected Button btnAjouter;
    protected Button btnModifier;
    protected Button btnSupprimer;
    protected Button btnEnregistrer;
    protected Button btnAnnuler;

    public void changementDeVisibiliteSurBouton()
    {
        if (btnAnnuler.isVisible() == false)
        {
            btnAnnuler.isVisible() = true;
            btnAnnuler.isCancelButton() = false;
            btnEnregistrer.isVisible() = true;
            btnEnregistrer.isCancelButton() = false;

            btnAjouter.isVisible() = false;
            btnAjouter.isCancelButton() = false;
            btnModifier.isVisible() = false;
            btnModifier.isCancelButton() = false;
            btnSupprimer.isVisible() = false;
            btnSupprimer.isCancelButton() = false;
        }
        else
        {

            btnAnnuler.isVisible() = false;
            btnAnnuler.isCancelButton() = false;
            btnEnregistrer.isVisible() = false;
            btnEnregistrer.isCancelButton() = false;

            btnAjouter.isVisible() = true;
            btnAjouter.isCancelButton() = true;
            btnModifier.isVisible() = true;
            btnModifier.isCancelButton() = true;
            btnSupprimer.isVisible() = true;
            btnSupprimer.isCancelButton() = true;

        }
    }

    public void changementDeVisibiliteSurBouton()
    {
        if (btnAnnuler.Visible == false)
        {

            btnAnnuler.Visible = true;
            btnEnregistrer.Visible = true;

            btnModifier.Visible = false;
        }
        else
        {
            btnAnnuler.isVisible() = false;
            btnAnnuler.isCancelButton() = false;
            btnEnregistrer.isVisible() = false;
            btnEnregistrer.isCancelButton() = false;

            btnAjouter.isVisible() = true;
            btnAjouter.isCancelButton() = true;
            btnModifier.isVisible() = true;
            btnModifier.isCancelButton() = true;
            btnSupprimer.isVisible() = true;
            btnSupprimer.isCancelButton() = true;

            btnAnnuler.Visible = false;
            btnEnregistrer.Visible = false;

            btnModifier.Visible = true;

        }
    }

    @FXML
    protected void onButtonClickAjouter() {
        changementDeVisibiliteSurBouton();
    }


*/


}
